# 23. 기기발신 휴대폰 번호 인증 (mobile-verify)

> **SDK 아님 · 게이트웨이 내장 REST.** 앱 사용자가 **자기 폰에서 문자 한 통을 보내면**
> 서버가 그 문자의 **발신번호를 관측해** 그 사람의 휴대폰 번호를 확정합니다.
> 사용자는 번호를 **타이핑하지 않고**, 서버는 사용자의 **주장을 믿지 않습니다.**
>
> 게이트웨이 `1.4.15.170+` · 토큰 마스킹·전용 보존기간은 `1.4.15.297+`

---

## 1. 무엇을 푸는 기능인가

착신전환 목적지를 **잘못 넣으면 회사 전화가 모르는 사람에게 갑니다.** 실측(2026-08-17)에서
목적지가 4자리 내선으로 저장된 사례가 있었고, 형식 검증으로는 *"형식이 맞는 남의 번호"* 를
막을 수 없습니다.

그래서 번호의 **출처**를 검증합니다. 방법은 두 가지뿐입니다.

| 출처 | 검증 방식 |
|---|---|
| 운영자 입력 | 대시보드에서 관리자가 직접 넣음 (seat `mobileNumber`) |
| **기기발신 문자 인증** | ⭐ **이 문서** — 그 폰에서 보낸 문자의 발신번호를 서버가 관측 |

⚠️ **이 둘로 좁히고 나면 수동 입력을 닫을 수 있습니다** — `manual-destination-policy`(§7).

---

## 2. ⚠️ 먼저 — 이 기능이 **하지 않는 것**

*"휴대폰 번호 인증"* 이라는 이름 때문에 넓게 오해되기 쉬워 먼저 못 박습니다.

- ❌ **일반 사용자 회원가입 본인확인이 아닙니다.** 호출 주체는 **이미 존재하는 모바일 seat**
  입니다(seat 토큰 필수). 계정을 만드는 수단이 아닙니다.
- ❌ **임의의 번호를 인증해 주지 않습니다.** 인증되는 것은 *"이 문자를 보낸 그 폰의 번호"*
  이고, 그 값은 **호출한 seat 에 귀속**됩니다.
- ❌ **본인(사람) 확인이 아닙니다.** 확인하는 것은 **기기(회선)** 이지 신원이 아닙니다.
  주민번호·통신사 본인확인과 다른 층입니다.
- ❌ **국제 번호를 다루지 않습니다.** 관측된 발신번호가 그대로 저장되며, 인입 SMS 경로가
  받는 형태에 종속됩니다.

**현재 이 값(`seat.mobileNumber`)을 실제로 소비하는 곳은 하나입니다** — 수신 모드 저장 시
**착신전환 목적지의 초기값**(`resolveCFDestination` 의 마지막 폴백). ⚠️ 그리고 이것은
**초기값이지 동기화가 아닙니다** — 사용자가 앱에서 다른 번호(집 전화 등)로 바꿨다면 서버가
되돌리지 않습니다.

> ⚠️ 앞으로 늘어날 수는 있지만, **지금 있는 소비처는 위 한 곳**입니다. "다양한 용도" 로
> 설계에 반영하지 마십시오.

---

## 3. 흐름

```
앱                          게이트웨이                     통신사 ─→ PBX ─→ 게이트웨이
 │                              │
 │ ① POST …/mobile-verify/start │
 │─────────────────────────────>│  토큰 발급(12자, TTL 3분, seat 당 1개)
 │<─────────────────────────────│  {token, to, body, expiresAt, ttlSeconds,
 │                              │   inboundReady, inboundStatus}
 │                              │
 │ ② 문자앱을 연다               │
 │   sms:<to>&body=<body>       │   ⭐ 사용자는 [전송]만 누른다
 │                              │
 │  ─── 사용자 폰에서 실제 발송 ───────────────────────────────────>│
 │                              │                                  │
 │                              │<── AMI UserEvent(DvgSmsIn) ──────┘
 │                              │   토큰 일치 → 그 문자의 **발신번호**를 채택
 │                              │   → seat.mobileNumber 확정 · 무음 푸시 mobile_verified
 │                              │
 │ ③ GET …/mobile-verify/status │
 │─────────────────────────────>│
 │<─────────────────────────────│  {state:"verified", mobileNumber:"010…"}
```

### 본문의 브랜드 낱말 — 온프렘이 바꿉니다 (`1.4.15.310+`)

```
{접두사} 본인확인 {토큰}          기본 접두사: MAKECALL
```

`GET|PUT /api/v1/config/verify-prefix`(admin) · 대시보드 "✉️ 문자 접두사" ·
부팅 시드 `GW_SMS_VERIFY_PREFIX`.

⚠️ **`본인확인` 은 바꿀 수 없습니다** — 그 문구가 인입 문자를 알아보는 **기준점**이라,
열어 두면 파서가 관대해져 **고객의 일반 문자를 인증 문자로 오인**합니다.

⭐ **바꾸면 직전 값을 기억합니다.** 접두사는 **발급 때**와 **인입 때(몇 분 뒤)** 두 번
쓰이므로, 그 사이에 바뀌면 옛 문구로 나간 문자의 **토큰이 평문으로 저장**됩니다.
마스킹은 **현재·직전·기본** 셋을 모두 인식합니다.

⭐ **본문은 서버가 완성합니다.** 앱이 조립하면 문구가 앱 버전마다 갈리고, 서버 파서가 그만큼
관대해져야 하는데 **관대한 파서는 오탐을 만듭니다.** 응답의 `body` 를 **그대로** 쓰십시오.

---

## 4. API

두 엔드포인트 모두 **모바일 seat 토큰**(`api.accessToken`)으로 호출합니다.
관리자 대행 개념이 없습니다 — *"그 사람이 자기 폰에서 보낸다"* 가 이 기능의 본질입니다.

### 4-1. `POST /api/v1/softphone/mobile-verify/start`

요청 본문 없음.

```json
{
  "token":         "Y9R6-Y5S8-H4QC",
  "to":            "07045144801",
  "body":          "MAKECALL 본인확인 Y9R6-Y5S8-H4QC",
  "expiresAt":     1756800000,
  "ttlSeconds":    180,
  "inboundReady":  true,
  "inboundStatus": "patched"
}
```

| 필드 | 뜻 |
|---|---|
| `token` | 발급 토큰(표시형). ⚠️ **화면에 표시하지 마십시오** — 노출 창을 줄이는 것이 이 설계의 전제입니다 |
| `to` | 문자를 **받을 번호** = 그 seat 의 수신 DID |
| `body` | 문자 본문 — ⭐ **그대로** 보내십시오(손으로 고치면 서버 파서가 못 찾습니다) |
| `expiresAt` | 만료 (Unix epoch 초) |
| `ttlSeconds` | 유효 기간(초). 현재 **180**(3분) — ⚠️ **seat 축의 값**입니다(아래) |
| `inboundReady` | 인입 SMS 경로가 준비됐는가 — `false` 면 문자를 보내도 **도달하지 않습니다** |
| `inboundStatus` | `patched` / `unpatched` / `unknown` |

⚠️ **`inboundReady` 는 `unknown` 에서 `true`** 입니다. *"증명된 미패치"* 만 막습니다 —
프로브 판정 불가에서 막으면 **정상 배포의 기능을 부정**하게 됩니다(모름 ≠ 없음).

**오류**

| 상태 | `code` | 뜻 · 조치 |
|---|---|---|
| `503` | `unavailable` | 기능 미구성(TTL 0 등) |
| `503` | `sms_disabled` | 이 테넌트에서 꺼짐(§6) |
| `403` | `not_mobile` 등 | 모바일 seat 토큰이 아님 |
| `429` | `rate_limited` | **내선당 3회/분** 초과 |
| `409` | `no_inbound_did` | 🔴 **그 seat 에 수신 DID 가 없습니다** — 보낼 곳이 없어 성립 불가. 앱은 수동 입력으로 폴백 |
| `500` | — | 토큰 발급 실패 |

⚠️ **재발급은 직전 토큰을 무효화**합니다(seat 당 pending 1개). 두 번 누르면 앞의 문자는
쓸모없어집니다.

### 4-2. `GET /api/v1/softphone/mobile-verify/status`

```json
{ "state": "pending", "mobileNumber": "", "expiresAt": 1756800000 }
```

| `state` | 뜻 |
|---|---|
| `none` | 발급 이력 없음 |
| `pending` | 살아있는 토큰이 있고 아직 소비되지 않음 |
| `verified` | ⭐ 확정됨 — `mobileNumber` 에 관측된 번호 |
| `expired` | 소비 없이 만료 |

- `mobileNumber` 는 **seat 의 현재 값**입니다 — 이번 인증으로 확정된 값이든 운영자가 넣은
  값이든, *"지금 착신전환 목적지 해석에 쓰이는 값"* 을 돌려줍니다.
- `expiresAt` 은 **살아있는 토큰이 있을 때만** 실립니다.
- ⚠️ 확정 통지는 **무음 푸시 `mobile_verified`** 로도 갑니다. 이 조회는 **폴백**입니다.
- ⚠️ 두 응답 모두 `Cache-Control: no-store` — 이 응답은 앱 진입점 노출을 결정하는
  **정책 게이트**라, 중간 캐시가 옛 `200` 을 들고 있으면 운영자가 꺼도 앱에는 켜진 것으로
  보입니다.

⭐ **앱은 `status` 로 지원 여부를 판정하십시오.** `start` 로 판정하면 **버튼을 그리려고
토큰을 발급**하게 됩니다.

### 4-2-1. ⚠️ TTL 3분은 **이 축(seat)의 값**입니다

앱 팀 되물음(2026-09-02)에 답하며 정리합니다.

| 축 | TTL | 상태 |
|---|---|---|
| **seat**(이 문서 · `.170`) | **3분** | 지금 도는 코드 |
| **청약**(회선 없는 신청자 · `start`/`status`) | 5분(계약 v1.1) | **미구현** |

⚠️ **3분의 근거를 정정합니다.** 코드 주석은 *"사용자 조작은 [전송] 한 번이라 3분이
충분하다"* 인데, 그 근거는 **축을 가르지 못합니다** — 청약 신청자도 [전송] 한 번만
누릅니다. 두 축을 가르는 진짜 차이는 **복구 가능성**입니다.

| | seat | 청약 |
|---|---|---|
| 사용자 | 앱을 이미 쓰는 사람 | **앱을 처음 여는 사람** |
| 실패 시 폴백 | 수동 입력(있음) | ❌ **없음** — 회선이 없어서 신청 중 |

즉 청약이 길어야 하는 이유는 *조작 횟수*가 아니라 **막다른 길의 대가**입니다.

🔴 **그런데 지금 코드는 이 분리를 표현할 수 없습니다.**

```
Store.ttl        하나뿐이다
New(ttl)         프로세스당 한 번 생성된다
Issue()          그 s.ttl 을 쓴다 — 호출자가 못 바꾼다
```

지금 청약 축을 같은 Store 에 얹으면 **한 상수가 성격이 다른 두 축을 재게 됩니다.**
청약 축 구현 시 **축별 Store** 또는 **`Issue` 가 TTL 을 받는** 형태로 가야 합니다.
⚠️ 지금 미리 만들지 않았습니다 — 청약 축이 아직 없어 추측 구현이 되기 때문이고,
**설계 제약으로만 기록**합니다.

### 4-2-2. 대행 발급 (admin / 테넌트 JWT)

앱이 **토큰을 화면에 띄우지 않는 설계**라, 앱을 그 seat 으로 등록하지 않으면 `body` 를
옮겨 적을 방법이 없습니다. 시험·지원 상황에서 *"seat 을 바꿨다 되돌린다"* 는 **그 사이
업무 전화를 못 받는다**는 뜻이라, 대행 경로를 둡니다(gw 1.4.15.301).

```
POST …/mobile-verify/start?tenantId=…&extension=0000    (admin)
POST …/mobile-verify/start?extension=0000               (테넌트 JWT — 자기 테넌트만)
GET  …/mobile-verify/status?tenantId=…&extension=0000
```

⭐ **새 권한이 아닙니다.** admin·테넌트 JWT 는 이미 `POST …/seats/{seatId}/mobile-number`
로 그 값을 **직접 쓸 수 있습니다.** 대행은 오히려 *"실제 폰이 보냈다"* 는 관측을 한 겹
더 요구하므로 **더 약한 권한**입니다.

| | 대행에서도 그대로 |
|---|---|
| 바뀌는 대상 | **그 seat 의 번호** (호출자의 것이 아님) |
| `mobile_verified` 무음 푸시 | ✅ **그 seat 으로** 발사 — 알림이 곧 방어 |
| 요청 제한 · 정책 게이트 | ✅ 동일 (대행이 우회로가 되지 않음) |
| 감사 | `[MOBILE-VERIFY] delegated start …` **반드시 기록** |

⚠️ **모바일 토큰이 `?extension=` 으로 남의 내선을 가리키면 `403 not_owner`** 입니다.
조용히 자기 내선으로 바꾸면 호출자는 **남의 내선을 인증한 줄 압니다.**

**스코프 오류**

| 상태 | `code` | 뜻 |
|---|---|---|
| `400` | `tenant_required` | admin 인데 `?tenantId=` 가 없음 |
| `400` | `extension_required` | 대행인데 `?extension=` 이 없음 |
| `403` | `not_owner` | 모바일 토큰이 남의 내선을 가리킴 |
| `403` | `seat_required` | 인증 없음 / 그런 seat 없음 |

### 4-3. 구버전 판별

`401` / `404` / `501` / `503` 은 전부 **"미지원"** 으로 통합 처리하십시오. 이 규약은 신규
모바일 엔드포인트마다 반복됩니다.

---

## 5. 전제 조건 — 안 되면 **아무 로그도 안 남습니다**

🔴 이 기능의 실패는 **조용합니다.** 아래 중 하나라도 빠지면 사용자는 문자를 보낸 뒤 영영
`pending` 이고, 서버 로그에도 흔적이 없습니다.

| # | 조건 | 확인 |
|---|---|---|
| 1 | **인입 SMS 415 패치** | `start` 응답의 `inboundStatus` · 대시보드 배지 |
| 2 | **그 seat 에 수신 DID** | 없으면 `409 no_inbound_did` |
| 3 | SMS 라우팅 구성 | 대시보드 "📩 SMS" |
| 4 | 테넌트 정책이 켜짐 | §6 |
| 5 | **발신번호 표시** | 표시제한(`From` 없음)이면 **fail-closed** — 토큰도 소비하지 않습니다 |

**진단 명령**

```bash
# ⬛ 게이트웨이 호스트 — 문자가 게이트웨이에 도달은 했는가
sudo journalctl -u dvgateway --since "-10m" | grep -iE 'DvgSmsIn|\[SMS\] received' \
  || echo "(인입 SMS 이벤트 0건 — 문자가 게이트웨이까지 오지 못했습니다)"
```

0건이면 문제는 토큰이 아니라 **인입 SMS 경로**(415 패치 · `message_context` · 수신 DID
매칭)입니다.

---

## 6. 테넌트 정책

`/api/v1/config/sms` 의 `mobileVerify` 로 테넌트별로 켜고 끕니다.

| 값 | 뜻 |
|---|---|
| `""`(미설정) | `enabled`(SMS 마스터)를 따름 |
| `allow` | **발신은 막고 인증만 열기** |
| `deny` | 인증만 끄기 |

- 꺼지면 두 엔드포인트가 **`503 sms_disabled`** → 앱이 "미지원" 으로 처리해 진입점이 사라집니다
  (앱 변경 불필요).
- ⚠️ 게이트는 **rate limit 앞**에 있습니다 — 꺼진 테넌트의 호출이 한도를 소모하면 켠 뒤에
  사용자가 막힙니다.
- ⚠️ **이미 발급된 토큰은 영향받지 않습니다** — 진입에서만 판정합니다(중간 차단은 신호 없는
  막다른 길입니다).

---

## 7. 짝이 되는 기능 — 수동 입력 차단

두 검증 경로가 자리잡으면 수동 입력을 닫을 수 있습니다.

```
PUT /api/v1/tenants/{id}/seats/manual-destination-policy   {"policy":"deny"}
POST …/seats/{seatId}/manual-destination                    {"allowed":true}   # seat 예외
```

앱은 `GET /softphone/inbound-mode` 의 **`manualDestinationAllowed`** 로 입력칸을 숨기고,
**서버도 거절**합니다(`403 manual_destination_denied`) — 화면 숨김만으로는 구버전 앱과 직접
호출이 남습니다.

⚠️ **`deny` 로 바꾸기 전에 두 경로의 충족률을 반드시 측정하십시오.** 둘 다 불가능한 seat
(415 미패치 · 수신 DID 없음 · 표시제한 · 해외 회선)은 **번호를 넣을 방법이 아예 없어집니다.**
수동 입력 차단이 **무장 차단이 되면 안 됩니다** — 그건 전화를 못 받는 상태입니다.

---

## 8. ⚠️ 위협 모델 — 알고 쓰십시오

바인딩은 **`토큰 → seat`** 이고 번호는 **관측값**입니다. 즉 *"그 폰에서 보내야만 가능"* 이
아닙니다 — **토큰이 유출되면 공격자의 폰이 그 seat 의 착신전환 목적지가 됩니다.**
토큰은 문자앱 작성창에 표시되므로 **화면 한 장**이면 새어 나갑니다.

방어를 엔트로피가 아니라 **창(window)과 가시성**에 둡니다.

| 방어 | 값 |
|---|---|
| TTL | **3분** |
| 1회용 | seat 당 pending 1개 — 재발급이 직전 토큰을 무효화 |
| 발급 상한 | 내선당 **3회/분** |
| 표시제한 발신 | **fail-closed** — `From` 이 비면 토큰도 소비하지 않음 |
| **변경 통지** | ⭐ 확정 시 **무음 푸시 + 감사 로그** — 조용히 바뀌는 것이 그 공격의 전제라 **알림이 곧 방어** |

⚠️ 그래서 `mobile_verified` 는 **운영자 푸시 토글 목록에 넣지 않습니다**(끌 수 없습니다).

### 8-1. ⭐ 문구 규율 — **"본인인증"이라 부르지 않습니다**

이 기능이 확인하는 것은 **회선(기기) 소유**이지 **신원**이 아닙니다. 국내에서 *"본인확인"*
은 **본인확인기관** 영역이라, 같은 이름으로 부르면 사용자에게도 계약서에서도 사실과
다릅니다. 화면 문구는 **"명의 확인은 통신사가 진행합니다"** 를 유지하십시오.

---

## 9. 개인정보 — 무엇이 어디에 남는가 (`1.4.15.297`)

인증 문자는 **신청자/사용자의 휴대폰 번호**를 발신번호로 갖고 들어옵니다.

- **본문의 토큰은 저장 전에 가립니다.** 원문 저장은 곧 **자격증명 로깅**이었습니다.
  TTL 3분은 *"짧게 유효한 자격증명"* 이지 *"자격증명이 아닌 것"* 이 아닙니다.
  ⚠️ 가리는 것은 **우리 접두사가 있는 문자만** 입니다 — 토큰 모양(`XXXX-XXXX-XXXX`)은
  주문번호·쿠폰에도 흔해서, 모양만으로 가리면 **고객의 정상 문자를 훼손**합니다.
- **인증 문자 전용 보존기간** `GW_SMS_VERIFY_RETENTION_DAYS`(기본 **30일**).
  ⚠️ **`GW_SMS_RETENTION_DAYS` 를 낮추는 것은 답이 아닙니다** — 그것은 설치 전체에 걸려
  **고객의 일반 문자까지** 지웁니다. **축이 다릅니다.**
- 이력에는 `kind:"mobile_verify"` 가 붙습니다(**필드 부재 = 일반 문자**). 앱은 문자함에서
  접거나 라벨을 붙일 수 있습니다.
- `sms_received` 푸시는 **발사하지 않습니다**(시스템 핸드셰이크이지 사용자 문자가 아닙니다).

🔴 **수신 seat 배치에 주의하십시오.** 인증 문자를 받는 seat 을 **고객 테넌트 안에 두면**
그 테넌트 관리자가 문자함에서 **번호 목록을 봅니다**(`GET /api/v1/sms` 는 테넌트 JWT 에
테넌트 전체를 보여 줍니다 — 모바일 토큰만 본인 내선 스코프). 일상 열람자가 없는 **전용
테넌트**에 두십시오.

---

## 10. 흔한 실패

| 증상 | 원인 | 조치 |
|---|---|---|
| `start` 는 되는데 계속 `pending` | 문자가 게이트웨이에 미도달 | §5 진단 명령 → 0건이면 415 패치·`message_context` |
| `409 no_inbound_did` | 그 seat 에 수신 DID 없음 | 대시보드 seat 표에서 DID 배정, 또는 수동 입력 폴백 |
| `503 sms_disabled` | 테넌트 정책 꺼짐 | §6 `mobileVerify` |
| `429 rate_limited` | 3회/분 초과 | 1분 대기 |
| 문자를 보냈는데 `expired` | TTL 3분 초과 | ⭐ `start` **직후** 보내십시오. 게이트웨이 재시작도 토큰을 날립니다(메모리 전용) |
| 표시제한으로 보냄 | `From` 없음 → fail-closed | 발신번호 표시를 켜고 재시도 |

---

## 11. 실패율은 어디서 보나 · OTP 를 붙일 것인가

```
GET /api/v1/softphone/mobile-verify/meter        (admin, 읽기 전용)
→ { supported, since, starts, issued, startFailures{}, verified, expired, anonymousRejected }
```

⭐ **분모(`starts`)를 함께 봅니다.** 실패 3건은 그 자체로 아무 뜻이 없습니다 — 3/5 와
3/5000 은 정반대의 결론을 부릅니다.

| 값 | 읽는 법 |
|---|---|
| `startFailures.no_inbound_did` | 그 seat 에 수신 DID가 없어 **시작조차 못 한** 수 |
| `anonymousRejected` | 표시제한 발신 — ⭐ **인입 관측으로는 구제 불가한** 사용자 |
| `expired` | ⚠️ *"문자가 안 왔다"* + *"사용자가 안 보냈다"* 의 **합집합 상한** (서버는 [전송]을 눌렀는지 모릅니다) |

⚠️ **인메모리입니다** — 재시작하면 `since` 가 다시 시작합니다. 이 숫자로 *"지금 그렇다"* 를
말하려면 창을 함께 보십시오.

### OTP 로 바꿀 수 있나

기술적으로는 어렵지 않습니다(발급·TTL·1회용·상한이 이미 있고 SMS 발송도 있습니다).
⚠️ **그런데 방향이 정반대입니다.**

|  | 지금 (인입 관측) | OTP |
|---|---|---|
| 문자를 보내는 쪽 | **사용자** | **우리** |
| 증거 | 통신사가 붙인 **발신번호** | 사용자가 **입력한 값** |
| ⭐ 문자 비용 | **0원** | **시도마다 과금** |

OTP 는 **요청 하나가 곧 돈**이고, 그게 그대로 공격 표면입니다(상한 없으면 SMS 폭탄 =
요금 청구). 그리고 *"휴대폰 인증"* 은 한국에서 **본인확인기관** 영역과 겹치므로,
우리가 하는 **회선 소유 확인**과 같은 이름으로 부르면 계약서에서 문제가 됩니다.

⭐ 그래서 순서는 **① 이 미터로 실패율을 세고 → ② 유의미하면 폴백으로만 추가**입니다.
0에 가까우면 **안 만드는 것이 맞습니다.** 만들더라도 **대체가 아니라 폴백**입니다 —
인입이 안 되는 곳에서 발신은 되므로, 두 방향이 서로의 폴백입니다.

---

## 11-1. 청약(서비스 신청) 축 — 별개 표면 (`1.4.15.303+`, **기본 꺼짐**)

이 문서 본문은 **seat 축**입니다. 청약 신청자는 **회선이 없어서 신청하는 사람**이라 seat 도
내선도 없어, 별도 표면을 씁니다.

```
POST /api/v1/verify/contact/start     (HMAC)  → {ref, to, body, expiresAt, ttlSeconds,
                                                      inboundReady, inboundStatus}
GET  /api/v1/verify/contact/status    (HMAC)  → {ref, state, mobileNumber?}
```

| | seat 축 | 청약 축 |
|---|---|---|
| 주체 | 모바일 seat 토큰(내선) | 소비처의 **불투명 `ref`** |
| 인증 | seat 토큰 / admin 대행 | **HMAC** 서버-투-서버 |
| **TTL** | **3분** | **5분** |
| 결과 | `seat.mobileNumber` 에 확정 | **10분 보존** + `?consume=1` 즉시 폐기 |
| 활성 | 기본 켜짐(테넌트 정책) | **기본 꺼짐 — 라우트 미등록** |

⭐ **TTL 이 다른 근거는 조작 횟수가 아니라 복구 가능성입니다** — seat 사용자는 실패해도
수동 입력 폴백이 있지만, 청약자는 **회선이 없어서 신청 중**이라 없습니다.

### ⚠️ 켜는 순서 — **서비스 seat 이전이 먼저입니다**

```
① 서비스 확인 seat 을 전용 테넌트로 이전
② GW_SUBSCRIPTION_VERIFY_ENABLED=true
```

🔴 **순서를 바꾸면 안 됩니다**(앱팀 지적 2026-09-02) — 켠 뒤에 옮기면 **그 사이에 들어온
신청자 번호가 옛 테넌트 문자 이력에 남습니다.** 그 테넌트 대시보드 로그인을 가진 사람은
`GET /api/v1/sms` 로 그것을 봅니다. 이전은 **되돌릴 수 있지만 이미 남은 기록은 아닙니다.**

⚠️ 시험 왕복도 마찬가지입니다 — 시험 문자의 발신번호가 남습니다.

🔴 **`service_did_unbound`(409)** — 서비스 확인 seat(§4-2-2 · `/config/service-verify-seat`)이
없으면 **토큰을 발급하지 않습니다.** 발급해 두면 사용자는 문자를 보낸 뒤 영영 `pending`
이고 **서버 로그에도 안 남습니다.**

**서명**

| | 서명 대상 | 신선도 | 재생 거부 |
|---|---|---|---|
| `start`(POST) | `rawBody` | `issuedAt` ±300초 | `requestId` |
| `status`(GET) | **정규화된 쿼리 문자열** | `issuedAt` **±60초** | — (창으로 대체) |

정규화 = 쿼리 키를 정렬해 `k=v` 를 `&` 로 이음(값은 디코드된 원문, 같은 키가 여럿이면
값도 정렬).

🔴 **`ref` 만 서명하면 안 됩니다** — `ref` 는 초안당 **고정값**이라 서명도 고정이 되어
**한 번 새면 영구 조회**가 됩니다(`.303` 의 실제 결함, `.304` 에서 수정).

⚠️ GET 창을 60초로 좁힌 이유: 조회는 폴링이라 서명이 훨씬 자주 만들어져 캡처 기회가
많습니다.

🔴 **60초의 전제 — 서명을 만드는 쪽의 시계가 정확해야 합니다.** 현재 소비처는 서명을
**서버(Cloud Function)** 에서 만듭니다(앱팀 확인 2026-09-02). ⚠️ **서명이 기기로
내려가면 이 값을 다시 봐야 합니다** — 기기 시계가 1분만 틀어져도 **모든 조회가 거부**
됩니다. 창을 늘리는 것이 아니라 **서명 위치를 유지하는 것**이 답입니다.

### `requestId` 재생 거부를 GET 에 두지 않은 이유

⭐ **부작용이 없기 때문입니다**(비용은 그 다음 이유입니다):

```
POST(발급)  재생 = 새 토큰이 생긴다        → 부작용 있음 → requestId 필요
GET(조회)   재생 = 같은 값을 다시 읽는다   → 부작용 없음 → 얻을 게 없다
```

⚠️ **예외 하나 — `?consume=1` 은 부작용이 있습니다.** 캡처된 서명을 60초 안에 재생하면
**정당한 소비처보다 먼저 결과를 지울 수 있습니다**(그쪽은 `none` 을 받고 재발급하게 됩니다).

그럼에도 두지 않은 이유: 그 서명을 캡처한 상대는 **이미 번호를 읽을 수 있고**, 남는 피해는
*"정당한 소비처가 한 번 더 발급해야 함"* 뿐입니다. 그리고 그 재생 창은 **60초**입니다.
⚠️ 다만 *"GET 은 부작용이 없다"* 를 **일반 규칙으로 쓰지 마십시오** — 이 표면에는 예외가
있습니다.

⚠️ 토큰 형식과 본문은 seat 축과 **공유**합니다 — 갈리면 인입 파서가 두 벌이 되고,
**관대한 파서는 오탐을 만듭니다.** 인입 매칭은 **seat 축이 먼저** 시도됩니다.

⭐ **계약 원문을 보유합니다** — [v1.1 전문](../dvg-contact-verify-contract-v1.1.md), 대조는
[부록](../dvg-contact-verify-contract-v11-diff.md). `1.4.15.305` 에서 여섯 군데를 정렬했고,
⚠️ **번호 표기(E.164 vs 국내)** 한 건이 미결입니다(부록 §3).

---

## 12. 실측

### 청약 축 (2026-09-03) — 전 구간 성공

HMAC 서명(POST rawBody · GET 정규화 쿼리) · ISO8601 신선도 · 인입 라우팅 · 토큰 매칭 ·
`observedFrom`/`observedAt` · `?consume=1` 즉시 폐기 · **`service_did_unbound` 409**
전부 확인했습니다. ⭐ `observedFrom` 은 **국내 표기 원값**(`010…`)으로 나왔습니다.

⚠️ **한도와 회전 유예 창은 단위 테스트만** 있고 실물로 안 밟았습니다.

### seat 축 (2026-09-02)

전 구간이 실기기로 한 번 돌았습니다 — 대행 발급을 써서 **기기를 건드리지 않고** 했습니다.

| 항목 | 결과 |
|---|---|
| 인입 라우팅 · 토큰 매칭 · 번호 관측 | ✅ |
| `kind: mobile_verify` 분류 | ✅ |
| **저장 본문 토큰 마스킹** | ✅ `MAKECALL 본인확인 ****` |
| 발신번호(관측 대상) 보존 | ✅ |

⚠️ **왕복 1회**입니다. 실패 경로(표시제한 · 만료 · 본문 변형)는 실기기로 밟아 보지
않았고, 그 분포는 §11 미터가 운영 중에 셉니다. 30일 보존 스윕의 실제 삭제는
**시간이 지나야** 확인됩니다.

---

## 관련 문서

- [21. SMS 발송·수신](21-sms.md) — 인입 SMS 경로·415 패치
- [18. 앱 푸시 / 알림](18-app-push-notifications.md) — `mobile_verified` 무음 푸시
